node_modules
packages/*/node_modules
dist
out
packages/*/out
packages/*/dist
.DS_Store
.prettierignore
.eslintcache
*.log*
.trae/
.cursor/
doc/
# Ignore temp artifacts
temp/
logs/
test-results/
playwright-report/
*.min.json
.claude/settings.local.json
.claude/
.agents/
.agent/

# Internal planning / reverse-engineering notes — not for distribution
.reveng/

# Secrets & local environment (safeguard — never commit these)
.env
.env.*
*.pem
*.p12
*.keystore

# E2E fixture dirs must never accumulate agent state (a test run that
# resolves the fixture as a project writes .wordbird/ into the repo).
packages/desktop/test/e2e/data/.wordbird/
